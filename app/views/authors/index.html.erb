<table id="example" class="table table-hover table-bordered" cellspacing="0" width="100%">
  <thead class="thead-dark">
    <tr>
      <th class="th-sm">Nombre
      </th>
      <th class="th-sm">Apellido
      </th>
      <% if current_user.admin %>
        <th class="th-sm">Acción
        </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @authors.each do |author| %>
    <tr>
      <td><%= author.name %>
      </td>
      <td><%= author.last_name%>
      </td>
      <% if current_user.admin %>
        <td><%= link_to 'Editar', edit_author_path(author), class: 'btn btn-primary btn-lg btn-block' %>
        </td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>