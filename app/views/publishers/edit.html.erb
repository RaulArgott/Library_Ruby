<% if current_user.admin %>
  <%= bootstrap_form_for(@publisher) do |f| %>
    <%= f.text_field :name, value: @publisher.name %>
    <%= f.text_field :city, value: @publisher.city %>
    <%= f.submit %>
  <% end %>
<% else %>
  <h1>Opcion solo para administradores</h1>
<% end %>