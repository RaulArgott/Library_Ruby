<table id="example" class="table table-hover table-bordered" cellspacing="0" width="100%">
  <thead class="thead-dark">
    <tr>
      <th class="th-sm">Editorial
      </th>
      <th class="th-sm">Ciudad o País
      </th>
      <% if current_user.admin %>
        <th class="th-sm">Accion
        </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @publishers.each do |publisher| %>
    <tr>
      <td><%= publisher.name %>
      </td>
      <td><%= publisher.city %>
      </td>
      <% if current_user.admin %>
        <td><%= link_to 'Editar', edit_publisher_path(publisher), class: 'btn btn-outline-primary btn-lg btn-block' %>
        </td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>