<% if current_user.admin %>
  <%= form_for(@publisher) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.label :city %>
    <%= f.text_field :city %>
    <%= f.submit %>
  <% end %>
<% else %>
  <h1>Opcion para administradores</h1>
<% end %>