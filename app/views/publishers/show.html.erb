<p><%= @publisher.name %> <%= @publisher.city %></p>
<p><%= link_to 'Edit', edit_publisher_path(@publisher) %> <%= link_to  "Delete", publisher_path(@publisher), method: :delete, data: {confirm: 'Are you sure?'} %> </p>